<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CW2"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dashboard Activity -->
        <activity
            android:name=".DashboardActivity"
            android:exported="true" />

        <activity android:name=".upload_employee_activity"
            android:exported="true" />
        <activity android:name=".holiday_requests_activity"
            android:exported="true" />
        <activity android:name=".increment_salaries_activity"
            android:exported="true" />
        <activity android:name=".view_employees_activity"
            android:exported="true" />
        <activity android:name=".manage_notifications_activity"
            android:exported="true" />

    </application>
</manifest>
